services:
  sdk-docs:
    build:
      context: ..
      dockerfile: docs/Dockerfile
    ports:
      - "80:80"
    container_name: sdk-docs
    env_file:
      - .env
    environment:
      - DOCS_DOMAIN=${DOCS_DOMAIN}
