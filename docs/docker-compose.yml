services:
  sdk-docs:
    build:
      context: ..
      dockerfile: docs/Dockerfile
      args:
        - PYTHONPATH=${PYTHONPATH}
    ports:
      - "80:80"
    container_name: sdk-docs
    environment:
      - DOCS_DOMAIN=${DOCS_DOMAIN}
